# Server token proxy env
# Set your Spotify App client secret here. The proxy will use it to call accounts.spotify.com (server-side) to avoid CORS and keep the secret safe.
SPOTIFY_CLIENT_SECRET=<your_spotify_client_secret>
# Optional: port for the token proxy
PORT=5174
# Optional: origin allowed to call the proxy (set to your frontend origin)
ALLOWED_ORIGIN=http://localhost:5173

