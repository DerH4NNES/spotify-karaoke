// DeviceChooser is deprecated; device selection is now handled in the PlayPage in-page UI.
// Keep a minimal stub to avoid import errors from older references.

export default function DeviceChooser() {
  return null;
}
